/**
 * Utility functions for Fakultair FTI 2026 Linktree
 */

export function trackLinkClick(label, url) {
  console.log(`[Analytics] Link clicked: ${label} (${url})`);
  // Placeholder for future analytics integrations (e.g. Vercel Analytics, Google Analytics)
}
